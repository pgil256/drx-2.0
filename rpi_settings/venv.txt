  # Create and activate virtual environment
  python3 -m venv kneespa_env
  source kneespa_env/bin/activate

  # Install system packages
  sudo apt-get update
  sudo apt-get install -y python3-pyqt5 python3-vlc vlc

  # Create symlinks for system packages
  ln -s /usr/lib/python3/dist-packages/PyQt5 kneespa_env/lib/python3.7/site-packages/
  ln -s /usr/lib/python3/dist-packages/sip.cpython-37m-arm-linux-gnueabihf.so kneespa_env/lib/python3.7/site-packages/
  ln -s /usr/lib/python3/dist-packages/vlc.py kneespa_env/lib/python3.7/site-packages/

  # Install core NumPy and OpenCV with specific versions
  pip install numpy==1.20.3
  pip install opencv-python==4.5.5.64

  # Install remaining Python packages
  pip install pyserial>=3.5 configparser>=5.0.0 pytest>=7.0.0 pytest-qt>=4.0.0 pytest-mock>=3.8.0 pytest-cov>=3.0.0
  pip install RPi.GPIO

  echo "KneeSpa environment setup complete!"