Arduino Support Sketches
========================

This directory contains support sketches for calibration and scale diagnostics.

The motor-controller firmware is canonical in `main/motor/`, where it has a
PlatformIO project and tests. Do not add another motor sketch here.
