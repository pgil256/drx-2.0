# Runtime requirements for the KneeSpa application.
# Buster / Python 3.7 maintenance pins. Keep upgrades compatible with the deployed Pi.
# Test-only dependencies live in development/requirements-test.txt at the repository root.

# Hardware Control
RPi.GPIO==0.7.1
pyserial==3.5

# Environment Variables
python-dotenv==0.21.1

# Local QR generation for phone sign-in and patient registration (Python 3.7 compatible).
qrcode==7.4.2

# Configuration Management
configparser==5.3.0  # Legacy backport; Python 3 uses its built-in configparser.

# Video Playback
python-vlc==3.0.21203

# NOTES:
# ------
# PyQt5 MUST be installed via apt-get:
#   sudo apt-get install python3-pyqt5
