# Local workspace backups

Keep manually created ZIP archives and other workspace snapshots under `local/`.
This directory is ignored by Git and excluded from device deployment. Keep
device-specific originals in `devices/archive/` at the repository root.
