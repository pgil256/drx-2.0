Arduino Support Sketches
========================

`devices/maintenance/arduino/` contains support sketches for calibration and scale diagnostics.

The motor-controller firmware is canonical in `runtime/arduino/motor/`, where it has a
PlatformIO project; native tests live in `development/tests/firmware/`. Do not add another motor sketch here.
