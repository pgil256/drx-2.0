# Local design references

The ignored `local/` directory contains original design exports, mockup HTML,
the prototype `app/` and its `assets/`, screenshots, and associated source files.
Their relative directory layout is preserved so the HTML references still work.

The production UI is in `runtime/raspberry-pi/main/ui/`. These local reference
files are not part of the application or normal device sync.
