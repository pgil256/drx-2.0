[pytest]
markers =
    unit: Pure logic tests, no hardware or Qt
    integration: FakeArduino + Qt signal tests
    hardware: Requires real Pi + Arduino
testpaths = tests
; Per-test cap. The pty protocol integration tests legitimately run tens of
; seconds (real threads + serial waits); they carry explicit timeout markers.
timeout = 120
qt_api = pyqt5
