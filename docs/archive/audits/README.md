Historical Audit Archive
========================

This directory holds older analysis reports, fix summaries, and one-off audit notes
that are useful for archaeology but should not live at the repository root.

The current runnable application and tests live under `main/`, `tests/`, `tools/`,
and `rpi/`.
